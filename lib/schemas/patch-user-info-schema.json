{
  "description": "Update a user info object.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "email": {
      "email": {
        "type": "string",
        "format": "email",
        "description": "The new users email address"
      }
    }
  }
}