{
  "description": "Add a new user-info object",
  "required": [
    "email"
  ],
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "email": {
      "email": {
        "type": "string",
        "format": "email",
        "description": "The new users email address"
      }
    }
  }
}